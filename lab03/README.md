# Lab03 — Lexical Analysis

This folder contains a lexical analysis implementation used as a compiler front-end exercise.

Quick run:

```bash
gcc lexicalanalysis.c -o lexicalanalysis
./lexicalanalysis < input.txt
```

Files:
- `lexicalanalysis.c` — lexer source
- `input.txt` — sample source to tokenize
