# Two Pass — Alternate Folder

This folder mirrors the root two-pass assembler files.

Quick run:

```bash
gcc twopassassembler.c -o twopassassembler
./twopassassembler < input.txt > output.txt
```

Files:
- `twopassassembler.c` — assembler source
- `input.txt` — sample input
